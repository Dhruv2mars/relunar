{
  "name": "relunar-repo",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.13",
  "engines": {
    "node": ">=22.12.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "verify": "turbo run test typecheck build",
    "release:tag": "node scripts/release-tag.mjs"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "turbo": "^2.8.4",
    "typescript": "^6.0.3"
  }
}
